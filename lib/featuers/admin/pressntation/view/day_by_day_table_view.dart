import 'package:flutter/material.dart';

class DayByDayTableView extends StatelessWidget {
  const DayByDayTableView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}